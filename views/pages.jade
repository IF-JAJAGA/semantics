extends layout

block link
  -var active = '/pages'

block content
  h1= title
  table.lead.table.table-striped.table-bordered
    tr
      th ID
    each page in pages
      tr
        td
          a(href='/pages/' + page.id)= page.id
